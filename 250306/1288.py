# 1288. 새로운 불면증 치료법

# 불면증 강화법이 아닌지...
# 왜 불면증에 걸렸는지 조금은 알 것 같은...

t = int(input())

for testcase in range(1, t+1):
    n = input()

    # 나오는 숫자를 셈
    # 0~9까지 나오면 끝
    # 변수 만들어서 나올 때 할당(카운트)하기
    # 리스트로 변수?
    # 계산 후 결과값 -> 자리 별로 쪼갬 -> 일치하는 인덱스에 넣어주기
    # -> 모든 자리가 1 이상이면 끝
    # 너무 졸려서 못 풀겠어요 ㅜㅜ


    numbersappeared = [0] * 10
    unpackednumbers = list(map(int, list(n)))

    times = 2

    while 1:
        unpackednumbers * times:
            if 안 끝낫으면
                times += 1



        for i in range(10):
            numbersappeared[i] >= 1


    print(unpackednumbers)




    print(f'#{testcase} {answer}')